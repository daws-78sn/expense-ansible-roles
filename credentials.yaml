# copy encrypted password from ansible server and paste here
# which is nothing but mysql_root_password: ExpenseApp@1
# also you can enter ansible_user: ec2-user and ansible_password: DevOps321 in edit credentials.yaml in ansible server
# then remove user and password in config and inventory files
# https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html#hosts-and-groups
